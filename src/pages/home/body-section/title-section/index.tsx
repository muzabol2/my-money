import * as $ from "./styled";

const TitleSection = () => (
  <$.StyledContainer>
    <span>Date</span>
    <span>Transaction</span>
    <span>Category</span>
    <span>Amount</span>
  </$.StyledContainer>
);

export default TitleSection;
