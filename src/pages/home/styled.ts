import styled from "styled-components";

const StyledContainer = styled.div`
  display: flex;
  flex-direction: column;
  width: 100%;
`;

const StyledErrorMsg = styled.p`
  margin-top: 20px;
  color: red;
`;

export { StyledContainer, StyledErrorMsg };
